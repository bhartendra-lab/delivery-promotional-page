/**
 * Re-exports the canonical Icon* barrel so existing imports in this
 * directory keep working unchanged. New code should import from
 * "@/components/ui/icons" directly.
 */
export * from "@/components/ui/icons";
